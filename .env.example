# Development Environment Variables
DATABASE_URL=postgresql://username:password@localhost/soundscape_development

# Email Configuration  
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_DOMAIN=soundscape.com

# AI Configuration
GEMINI_API_KEY=your-gemini-api-key-here

# Redis Configuration (for caching and rate limiting)
REDIS_URL=redis://localhost:6379/0

# Production Environment Variables for Render
# DATABASE_URL=postgresql://username:password@hostname:port/database_name (provided by Render)
# RAILS_ENV=production
# SECRET_KEY_BASE=your_secret_key_base_here (auto-generated in render.yaml)
# RAILS_MASTER_KEY=your_master_key_here (from config/master.key)
# CORS_ALLOWED_ORIGINS=https://your-frontend-domain.vercel.app
# RAILS_SERVE_STATIC_FILES=enabled
# RAILS_LOG_TO_STDOUT=enabled